import { EIconColor, EIconName } from '@/components/Icon';

export type TAvatarProps = {
  image?: string;
  size?: number;
  iconNameDefault?: EIconName;
  iconColorDefault?: EIconColor;
  className?: string;
};
