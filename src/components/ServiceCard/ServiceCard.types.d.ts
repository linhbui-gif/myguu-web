export type TServiceCardProps = {
  verify?: boolean;
  discountPercent?: number;
  address?: string;
  border?: boolean;
  sellingPrice?: number;
  retailPrice?: number;
  subtitle?: string;
  vertical?: boolean;
  showQuantity?: boolean;
};
